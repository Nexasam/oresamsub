<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>Standalone credentials</title><style>body{font:15px system-ui;background:#f4f7fb;padding:32px}main{max-width:800px;margin:auto;background:white;padding:28px;border-radius:16px}code{display:block;padding:14px;background:#111827;color:#dbeafe;overflow-wrap:anywhere;margin:8px 0 22px}.warn{color:#9a3412}</style></head><body><main><h1>{{ $site->business_name }} credentials</h1><p class="warn"><b>Copy these values now.</b> They will not be shown again.</p>@if($credentials['api_token'])<h2>Standalone API token</h2><code>{{ $credentials['api_token'] }}</code>@endif @if($credentials['webhook_secret'])<h2>Webhook signing secret</h2><code>{{ $credentials['webhook_secret'] }}</code>@endif<a href="{{ route('admin.standalones.show',$site) }}">Continue to standalone</a></main></body></html>
